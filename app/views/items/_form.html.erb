<%= form_for([@watchlist, @watchlist.items.build]) do |form| %>
  <% if @watchlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(watchlist.errors.count, "error") %> prohibited this item from being saved:</h2>
      <ul>
        <% watchlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div>Add New Entry</div>

  <div class="field">
    <%= form.text_field :title, placeholder: "Title" %>
    <%= form.text_field :year, placeholder: "Year" %>
    <%= form.text_field :genre, placeholder: "Genre" %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>